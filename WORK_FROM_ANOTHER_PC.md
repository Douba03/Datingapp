# Work From Another PC 🖥️

Your code has been pushed to GitHub! Here's how to work from another PC.

---

## ✅ What Was Pushed:

- **Repository:** https://github.com/Douba03/Datingapp.git
- **Branch:** master
- **Commit:** f518f61 - "Update: Premium features, dark mode, IAP setup, and build fixes"
- **Files:** 120 files changed (8,739 insertions, 4,422 deletions)

---

## 📥 Setup on Another PC:

### **Step 1: Clone the Repository**

```bash
git clone https://github.com/Douba03/Datingapp.git
cd Datingapp
```

---

### **Step 2: Install Dependencies**

```bash
npm install
```

This will install all packages from `package.json`.

---

### **Step 3: Set Up Environment Variables**

Create a `.env.local` file in the root directory:

```env
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

**Important:** Don't commit `.env.local` to GitHub (it's in `.gitignore`)

---

### **Step 4: Generate Native Projects (If Needed)**

If you want to build locally:

```bash
npx expo prebuild
```

This creates `android/` and `ios/` folders.

---

### **Step 5: Run the App**

```bash
# Start Expo dev server
npx expo start

# Or run on specific platform
npx expo start --android
npx expo start --ios
npx expo start --web
```

---

## 🔄 Daily Workflow:

### **Pull Latest Changes:**
```bash
git pull origin master
npm install  # If package.json changed
```

### **Make Changes & Push:**
```bash
# 1. Make your changes
# 2. Stage changes
git add .

# 3. Commit
git commit -m "Your commit message"

# 4. Push
git push origin master
```

---

## 📋 What's Included:

✅ All source code (`src/` folder)  
✅ Configuration files (`app.json`, `package.json`, etc.)  
✅ Documentation (guides and fixes)  
✅ SQL scripts  
✅ Build configuration  

---

## 🚫 What's NOT Included (by design):

❌ `node_modules/` - Install with `npm install`  
❌ `.expo/` - Generated folder  
❌ `android/` - Generated by `expo prebuild`  
❌ `ios/` - Generated by `expo prebuild`  
❌ `.env.local` - Your secrets (create manually)  
❌ Build artifacts  

---

## 🔐 Important Files to Set Up:

1. **`.env.local`** - Supabase credentials
2. **`android/local.properties`** - Android SDK path (if building locally)
3. **Android Studio** - For local Android builds

---

## 💡 Quick Commands:

```bash
# Clone repository
git clone https://github.com/Douba03/Datingapp.git
cd Datingapp

# Install dependencies
npm install

# Start development
npx expo start

# Pull latest changes
git pull origin master

# Push your changes
git add .
git commit -m "Your message"
git push origin master
```

---

## 🐛 Troubleshooting:

**"Module not found"**
- Run: `npm install`

**"Expo not found"**
- Run: `npm install -g expo-cli` or use `npx expo`

**"Can't connect to Supabase"**
- Check `.env.local` file exists with correct credentials

**"Android build fails"**
- See `FIX_LOCAL_BUILD.md` and `FIX_NDK_ERROR.md`

---

## 📚 Documentation Files:

- `LOCAL_BUILD_GUIDE.md` - How to build locally
- `FIX_LOCAL_BUILD.md` - Fix SDK errors
- `FIX_NDK_ERROR.md` - Fix NDK errors
- `NATIVE_IAP_SETUP.md` - Set up in-app purchases
- `BUILD_FIXES.md` - Build troubleshooting

---

**Your code is on GitHub!** 🎉  
**Repository:** https://github.com/Douba03/Datingapp.git





